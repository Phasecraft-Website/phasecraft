import { global } from './parts/global';
import { media } from './parts/media';
import { colors, colorVariables, sizeVariables } from './parts/variables';
import { transitions } from './parts/transitions';
import { fontFamilies, typography, link } from './parts/typography';
import { grid } from './parts/grid';
import { modal } from './parts/modal';
import { button } from './parts/button';

const theme = {
  global,
  media,
  colors,
  colorVariables,
  sizeVariables,
  transitions,
  fontFamilies,
  typography,
  link,
  grid,
  modal,
  button,
};

export default theme;
